# Dependencies / build
node_modules/
dist/
*.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code local / per-user state (don't ship dogfood artifacts)
.sentinel/
.skull/
.claude/settings.local.json
.claude/skull.json

# clone runtime state + secrets (NEVER commit)
.clone/
.env
.env.*
!.env.example
